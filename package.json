{
	"name": "basedash-full-embed",
	"version": "1.0.0",
	"description": "Test harness for Basedash JWT-based embedding feature",
	"type": "module",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"build": "bun build src/index.ts --outdir dist"
	},
	"dependencies": {
		"hono": "^4.11.1",
		"jose": "^5.2.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@vercel/node": "^3.2.0",
		"vercel": "^37.0.0"
	}
}
