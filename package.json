{
	"name": "embed-test",
	"version": "1.0.0",
	"description": "Test harness for Basedash JWT-based embedding feature",
	"type": "module",
	"scripts": {
		"dev": "bun run --hot src/index.ts"
	},
	"dependencies": {
		"jose": "^5.2.0"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
