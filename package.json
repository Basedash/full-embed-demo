{
	"name": "basedash-full-embed",
	"version": "1.0.0",
	"description": "Test harness for Basedash JWT-based embedding feature",
	"type": "module",
	"scripts": {
		"dev": "bun run --port 3001 --hot api/index.ts",
		"build": "bun build api/index.ts --outdir dist"
	},
	"dependencies": {
		"hono": "^4.11.1",
		"jose": "^5.2.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@vercel/node": "^3.2.0",
		"vercel": "^37.0.0",
		"typescript": "^5.9.3"
	}
}
